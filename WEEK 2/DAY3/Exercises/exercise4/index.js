const users = [];

if(users.length === 0 ) console.log("no one is online")

if(users.length === 1 ) console.log(user[0] + "are online")

if(users.length === 2 ) (console.log(users[0]) + "and" + users [1] + " are online")

if (users.length > 2) console.log("$(users[0]"), $(users[1]) , $(users.length - 2); "more are online"